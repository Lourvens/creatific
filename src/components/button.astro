<button>
  <slot />
</button>

<style>
  button {
    background-color: var(--black-color);
    border: none;
    cursor: pointer;
    padding: var(--space-sm);
    color: #fff;
    text-transform: uppercase;
    font-weight: 500;
  }
</style>
